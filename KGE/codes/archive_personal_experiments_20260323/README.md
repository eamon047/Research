Archived personal experimental code moved from `codes/` on 2026-03-23.

This folder keeps local experimental variants and backups so that the top-level
`codes/` directory stays close to the original RotatE project layout.

The main files intentionally kept in `codes/` are:
- `dataloader.py`
- `model.py`
- `run.py`
